{
  "name": "cDai-kovan-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ juanigallo/cDai-kovan-subgraph",
    "create-local": "graph create --node http://localhost:8020/ juanigallo/cDai-kovan-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ juanigallo/cDai-kovan-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 juanigallo/cDai-kovan-subgraph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.25.1",
    "@graphprotocol/graph-ts": "0.24.1"
  }
}
